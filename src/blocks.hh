#ifndef BLOCKS_HH
#define BLOCKS_HH

#include <array>
#include <string>
#include <cstdint>
#include <unordered_map>

using namespace std;

extern unordered_map<string, uint32_t> colors;

void init_block_list ();

#endif
