add_executable(pixel-terrain pixel_terrain.cc)
add_dependencies(pixel-terrain block_colors_data optlib)
if(ZLIB_USE_EXTERNAL_PROJECT)
  add_dependencies(pixel-terrain zlib)
endif()
if(PNG_USE_EXTERNAL_PROJECT)
  add_dependencies(pixel-terrain png)
endif()
target_link_libraries(pixel-terrain region_parser)
target_link_libraries(pixel-terrain logger)
target_link_libraries(pixel-terrain image_generator)
target_link_libraries(pixel-terrain block_server)
install(TARGETS pixel-terrain DESTINATION bin)

add_executable(nbt2xml nbt_to_xml.cc)
target_link_libraries(nbt2xml nbt_pull_parser)
install(TARGETS nbt2xml DESTINATION bin)

add_subdirectory(commands)
add_subdirectory(logger)
add_subdirectory(nbt)
